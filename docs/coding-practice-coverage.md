# Coding Practice Coverage

This note tracks important coding exercises that should exist before CodeQuest can feel like a complete beginner-to-practical path.

## Covered

- Terminal navigation and file operations
- Vim movement and editing fluency
- Git/GitHub basics and recovery
- Python fundamentals through Storybook
- Basic JavaScript syntax in Runescar
- AI-era CLI/API tool patterns in Oracle Tower

## Important Gaps

- JavaScript arrays and object records
- JSON parsing and structured API response handling
- Async/Promise flow, which is essential for web APIs and AI SDK calls
- Browser/UI event handling and state updates
- Error handling and validation
- Small project composition across files/modules

## Current Upgrade

Add JavaScript bridge missions to Runescar before the track shifts into Python:

- Array filtering and mapping over object records
- JSON parsing from API-like strings
- Promise/async handling without a real network dependency
- Component props as pure render functions
- UI state transitions with reducer-style functions
- Form validation and user-facing error messages
- Selector logic for deriving the next mission from app state

These bridge missions make Oracle Tower's SDK and streaming exercises less abrupt.

## Still Open

- Full JSX compilation in the code runner
- Multi-file module imports/exports in the code runner
- E2E exercises that connect UI, state, and persistence

## React Workshop Added

- `react-workshop-01`: props-driven component rendering
- `react-workshop-02`: state and click events
- `react-workshop-03`: conditional rendering
- `react-workshop-04`: named export/import matching
